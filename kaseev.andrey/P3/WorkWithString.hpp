#ifndef WORKWITHSTRING_HPP
#define WORKWITHSTRING_HPP
#include <cstddef>

namespace kaseev
{
  const char*  readLine();
  char* interleaveStrings(const char* str1,const char* str2);
  size_t lent(const char* str1);
}

#endif
