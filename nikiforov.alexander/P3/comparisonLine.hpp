#ifndef COMPARIZONLINE_HPP
#define COMPARIZONLINE_HPP
#include "addSizeLine.hpp"

namespace nikiforov {
  size_t resComparison(char* userStr, const char* staticStr, size_t userLen, size_t staticLen);
}

#endif
