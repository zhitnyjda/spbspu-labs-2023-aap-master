#ifndef FUNC_HPP
#define FUNC_HPP

namespace nikiforov {
  class Str
  {
  public:
    void AddElem(char*& arr, size_t& size, size_t& lenth);
    int result(char* str1, char* str2, size_t len1, size_t len2);
  private:
  };
}
#endif
