#ifndef SPIRAL_SUB_HPP
#define SPIRAL_SUB_HPP
#include <iostream>

namespace khomichenko
{
  void spiSub(size_t start, size_t inc, size_t rows, int * matrix);
  void callFuncSec(int* matrix, size_t cols, size_t rows);
}

#endif
