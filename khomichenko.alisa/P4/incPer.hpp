#ifndef INC_PER_HPP
#define INC_PER_HPP
#include <iostream>

namespace khomichenko
{
  void doInc(size_t start, size_t inc, size_t rows, int * matrix);
  void callFunc(int* matrix, size_t cols, size_t rows);
}

#endif
