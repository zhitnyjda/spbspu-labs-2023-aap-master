#ifndef DGTSND_HPP
#define DGTSND_HPP
#include <cstddef>
#include <cctype>

namespace kovshikov
{
  void withDigits(char * string1, const char * string2, char * output, size_t readStr2, size_t readStr1);
}

#endif
