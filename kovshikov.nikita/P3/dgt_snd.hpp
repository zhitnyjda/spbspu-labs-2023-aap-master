#ifndef DGTSND_HPP
#define DGTSND_HPP
#include <cstddef>
#include <cctype>

namespace kovshikov
{
  void withDigits(char * userString, const char * myString, char * output, size_t length, size_t read);
}

#endif
