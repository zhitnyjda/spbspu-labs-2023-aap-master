#ifndef MATRIX_HPP
#define MATRIX_HPP
#include <cstddef>

namespace susidko
{
  int CNT_ROW_NSM(const int * matrix, size_t rows, size_t cols);
  int NUM_COL_LSR(const int * matrix, size_t rows, size_t cols);
}
#endif
