#ifndef MATRIX_HPP
#define MATRIX_HPP
#include <cstddef>

namespace susidko
{
  int countUniqueRows(const int * matrix, size_t rows, size_t cols);
  int getColNumber(const int * matrix, size_t rows, size_t cols);
}
#endif
